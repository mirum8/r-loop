# Milestone report — {{.MilestoneName}}

Write a Markdown report of what {{.MilestoneName}} (phases {{.MilestonePhases}}) built to `{{.ReportPath}}`.

Build it from the landed commits of those phases and each phase's plan file under `.task-plans/`. For each phase: what it built, the tests that prove it, and the assumptions its plan recorded. End with what the milestone as a whole now makes possible.

Change no other file.
{{template "outcome" .}}
